const generateJsonWithLocal = async ({ systemInstruction, userPrompt, responseSchema, temperature }) => {
  throw new Error('[LocalProvider] Local LLM provider not implemented yet');
};

module.exports = {
  generateJsonWithLocal
};
