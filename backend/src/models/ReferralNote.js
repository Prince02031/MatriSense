// ReferralNote model - stores referral information for health workers
// TODO: Connect to MongoDB with Mongoose schema

module.exports = {
  // Fields:
  // _id, triageSessionId, healthWorkerNotes, followUpDate, status, createdAt, updatedAt
};
